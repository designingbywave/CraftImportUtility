{% import "_includes/forms" as forms %}

{{ forms.textField({
  first: true,
  label: "folderId",
  name: 'folderId',
  value: settings.folderId
}) }}

{{ forms.textField({
  first: true,
  label: "fieldId",
  name: 'fieldId',
  value: settings.folderId
}) }}

